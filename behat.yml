default:
    formatters:
        pretty: true

    autoload:
        '': '%paths.base%/features/bootstrap'

    suites:
        pomm:
            paths:    [ '%paths.base%/features/common' ]
            contexts: [ Context ]